# OpenEnv standard expects a client.py.
# The actual evaluation logic runs inference.py as requested by the competition rules.

from openenv.core.client import OpenEnvClient

# This is a dummy client file to satisfy the OpenEnv structure validation.
# The real agent code is in inference.py.
